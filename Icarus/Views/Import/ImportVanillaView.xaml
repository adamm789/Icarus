<UserControl x:Class="Icarus.Views.Import.ImportVanillaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Icarus.Views.Import"
             xmlns:import="clr-namespace:Icarus.ViewModels.Import"
             d:DataContext="{d:DesignInstance Type=import:ImportVanillaViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.Row="1" Grid.ColumnSpan="2">
        <Grid.Resources>

        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Command="{Binding GetVanillaModel}" Content="Add Vanilla Mdl"></Button>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AllRacesMdls}"
                  SelectedValue="{Binding SelectedRace}"
                  SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                  Text="Add Vanilla Model"/>
        <Button Grid.Row="0" Grid.Column="2" Command="{Binding GetVanillaMaterial}" Content="Add Vanilla Mtrl"/>
        
        <TextBox Grid.Row="1"  Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Text}" TextWrapping="NoWrap" Width="300"/>
        <Button Grid.Row="1" Grid.Column="2" Command="{Binding TryGetPathCommand}" Content="Try Get File"/>

        <!--
        <Menu Grid.Column="2">
            <MenuItem Header="Add Vanilla Mdl" ItemsSource="{Binding AllRacesMdls}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Command" Value="{Binding TestCommand, RelativeSource={RelativeSource AncestorType=local:ImportVanillaView}}"/>
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            

        </Menu>
        -->

        <!--
        <Menu Grid.Column="2">
            <MenuItem Header="Add Vanilla Mdl" ItemsSource="{Binding AllRacesMdls}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="{x:Type MenuItem}">
                        <Setter Property="Command" Value="{Binding TestCommand}"/>
                    </Style>
                </MenuItem.ItemContainerStyle>

            </MenuItem>
        </Menu>
        -->
        <!--
                  <Button Grid.Column="1" Command="{Binding GetVanillaMaterial}">Add Vanilla Material</Button>
            -->
    </Grid>
</UserControl>
