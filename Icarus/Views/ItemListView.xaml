<UserControl x:Class="Icarus.Views.ItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Icarus.Views"
             xmlns:items="clr-namespace:Icarus.ViewModels"
             d:DataContext="{d:DesignInstance Type=items:ItemListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--
    <Grid>
        <TreeView Grid.Row="1" VirtualizingStackPanel.IsVirtualizing="True">
        <TreeView.ItemContainerStyle>
            <Style TargetType="{x:Type TreeViewItem}">
                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                <Setter Property="Visibility" Value="{Binding Visibility}"/>
                <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            </Style>
        </TreeView.ItemContainerStyle>

        <TreeView.Resources>
            <HierarchicalDataTemplate 
                    DataType="{x:Type viewmodels:ItemListViewModel}"
                    ItemsSource="{Binding Items}">
                <StackPanel Height="Auto">
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} ({1})">
                                <Binding Path="Header"/>
                                <Binding Path="NumVisible"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </HierarchicalDataTemplate>
            <DataTemplate
                    DataType="{x:Type viewmodels:ItemViewModel}">
                <StackPanel>
                    <TextBlock Text="{Binding Name}"/>
                </StackPanel>
            </DataTemplate>
        </TreeView.Resources>
    </TreeView>
    </Grid>
    -->
<!--
    <TreeView>
        <TreeViewItem ItemsSource="{Binding Items}">
            <TreeViewItem.Header>
                <TextBlock>
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} ({1})">
                            <Binding Path="Header"/>
                            <Binding Path="NumVisible"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </TreeViewItem.Header>
            <TreeViewItem.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </TreeViewItem.ItemTemplate>
        </TreeViewItem>
    </TreeView>
    -->
    
</UserControl>
